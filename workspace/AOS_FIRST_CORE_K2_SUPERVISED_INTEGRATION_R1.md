# K2 — supervised integration package R1

> Historical preparation snapshot. Its pending/NOT_RUN labels describe the
> original subject and do not restart work. For the accepted result and next
> chat, read [current continuation](AOS_CORE_CONTINUATION_PLAN.md).

Subject: `AOS-FIRST-CORE-001/K2-SUPERVISED-INTEGRATION-R1`.
Status: PREPARED / WAIT_HUMAN_REVIEW. Execution: NOT_RUN.

## 1. Decision and outcome

The current Human reply «Принимаю» accepts preparation of the proposed first
supervised integration: exact Human task/candidate/action decision, existing
core admission/queue/ledger, bounded native sandbox, one exclusive creation of
byte `7`, and independent observation. It does not launch implementation or
the effect. Earlier S0/K1/support/Entry run grants are consumed.

This is a temporary supervised exception for this subject only. It does not
change [C-006/C-006A](../docs/02_Architecture.md#module-contracts),
[D1](../docs/00_Core.md#host-integration-d1), the
[13-family scope](../docs/01_Product.md#first-core-selected-scope), or
[native proof requirements](../docs/03_Development.md#native-host-bounded-probes).
Relevant owners: [FTR-006](../docs/06_Features.md#ftr-006-contract),
[FTR-009](../docs/06_Features.md#ftr-009-contract),
[FTR-010](../docs/06_Features.md#ftr-010-contract),
[FTR-019](../docs/06_Features.md#ftr-019-contract).
This derived handoff is not another canonical owner.

Desired result: a source-bound K1 task traverses preview, local registration,
durable COMMAND queue, fresh admission, operation ledger and independent result
validation under explicit manual supervision. Report `SUPERVISED_INTEGRATION`;
even a successful result does not complete full K2, NP-01, C-006A host conformance
or HD-28. Existing preview remains read-only and never grants execution.

## 2. Exact starting subject

All observations below are local, 2026-09-16; refresh before a future run.

| Binding | Value |
|---|---|
| Repository | `NMF13579/AOS-3` |
| W | `/Users/muhammed/Documents/GitHub/AOS-3-first-core-20260915` |
| Branch | `codex/first-core-20260915` |
| Base HEAD | `e99cc3ee03128deb4506bc268839ebd1f52a3f3a` |
| Accepted Entry source | `f6c896f74236acc7d80d18d449969d5ea791e6290e5fe59502148656f97012bb` |
| Manifest | `W/.first-core-state/K2-ENTRY-RUN-001.json`, key `candidate`, 19 relative first_core paths |
| Handoff R2 SHA256 | `4dc2b4a0fc43459de173385d3d68257e5dc4ba80d717655605717ce30103f8b3` |

All 19 current source hashes match that manifest. Candidate digest is SHA256 of
UTF-8 Python `json.dumps(manifest, sort_keys=True, ensure_ascii=False,
allow_nan=False)` with default separators. HEAD alone does not bind these
uncommitted files. Chat acceptance follows the record's historical
READY_FOR_HUMAN_REVIEW status; do not rewrite prior results to simulate capture.

Current owner SHA256 bindings:

| Owner | SHA256 |
|---|---|
| `docs/00_Core.md` | `212a2abfe7624a9c097eaab29894ddff4a1ea6716ac556d9601f38bfbef85c89` |
| `docs/01_Product.md` | `de2967a27f2cdd329608b6911a30cbd8541256215afaea52e4ad5e7b230b2ac8` |
| `docs/02_Architecture.md` | `caccecc5b0ad8ff11f3c7864f241c0dd7be230de8c8d07b64ea1505a28be01fc` |
| `docs/03_Development.md` | `a8b4b1d3d1c3a392f9e9cbb6bf556284a012b955e4c245c5c473c5ee426f0051` |

The current source accepts only FIXTURE_ONLY in state/admission/connectors/queue;
there is no real effect CLI. [Entry package](AOS_FIRST_CORE_K2_ENTRY_IMPLEMENTATION_R1.md)
and [preflight R2](AOS_FIRST_CORE_K2_PREFLIGHT_R2.md) remain historical.
Native approval-state evidence is in
`W/.first-core-scratch/host-native-r2/np01-app-server/approval-state-01/result.json`:
callback/replay/cancellation observations do not establish current AOS
authority/state/candidate binding. No new native experiment is proposed.

## 3. Two explicit execution boundaries

**A — implement and validate the temporary integration path.** A future grant
for this package may cover source edits, isolated fixture effects, local tests,
ordinary bounded corrections and a fresh read-only final reviewer. Finish with
the exact candidate, checks and one completed launch manifest for B, then STOP.
No real effect in B's root during A. This is implementation work, currently NOT_RUN.

**B — one actual supervised integration effect.** Only after A produces a
reviewable exact candidate may the Human authorize that candidate, exact input
bytes, command, action, service effects and finite budget. A's grant, test PASS
and generated manifest cannot authorize B. B changes no source. It ends after
independent observation/review, with no K3 or automatic continuation.

This separation binds the real-effect permission to existing reviewed code;
it does not require Human approval for individual edits or test corrections in A.
If A cannot produce the path in its bounded scope, return the precise blocker
instead of preparing another generic planning cycle.

## 4. Temporary profile and division of responsibility

Profile name for this subject: `K2-SUPERVISED-INTEGRATION/R1`.

| Boundary | Manual responsibility | Program/native contribution | Still unproven |
|---|---|---|---|
| Human source | Human grants exact package/candidate/action in this current App task; executor records verbatim decision and actual source locator/order without inventing unavailable IDs | Local digest and structural checks bind the recorded decision to reviewed inputs | Trusted native capture, authenticated current revocation |
| Candidate/action/state | Executor rereads source/input manifest and current decision immediately before dispatch; Human checks displayed scope | Core checks current local authority, task/state/event/action, expiry and unused envelope under its existing lock | Atomic native linkage of chat authority to the dispatched tuple |
| File/network scope | Executor uses exact allowlist and one designated writer; Human may stop | Reviewed child sandbox denies network and limits writes to the approved scratch root | Same-user malicious writer exclusion; no claim of full read isolation from write confinement |
| Persistence/delivery | Executor preserves material evidence and stops on uncertain effect | Existing store, registration, queue, admission consumption and ledger | Host-enforced single continuer, automatic wake/resume |
| Completion | Fresh read-only reviewer checks exact source/result; Human decides acceptance | Hashes, independent byte oracle, explicit check outcomes | Full K2/native conformance/autonomous readiness |

An agent-written record is a transcription, never proof of Human origin. A
matching supplied decision remains `MANUAL_SUPERVISION`, with native authenticity
UNKNOWN. The temporary entry must explicitly reject missing/inconsistent decision
bindings, but must not claim it can detect a perfectly forged Human transcript.
Only the supervised operator's current grant allows invocation. Normal preview,
FIXTURE_ONLY input, arbitrary JSON and a profile string do not acquire authority.

No host-adapter, external controller, daemon, scheduler, service, new queue family,
registry platform, role or dependency is introduced. Use the existing product
queue/admission/state mechanisms; bounded glue is part of this product slice.
Retain the normal unavailable-native-authority boundary outside this explicit
temporary mode. Do not globally relabel fixture results as production evidence.

## 5. A: implementation and write allowlist

Only the following paths under `W/first_core/` may change in a future A run:

| Path | Bounded purpose |
|---|---|
| `aos_core/supervised.py` — new | Temporary explicit integration entry; bind decision/input/source manifest and compose existing mechanisms |
| `tests/test_k2_supervised.py` — new | Independent expected integration outcomes and denial cases |
| `aos_core/__main__.py` | Add explicit `k2 supervised-run --input /absolute/request.json`; preserve preview/K1/support behavior |
| `aos_core/state.py` | Distinct temporary profile and immutable integration bindings using the existing store/lock; preserve FIXTURE_ONLY behavior |
| `aos_core/admission.py` | Profile-specific identity and current binding checks; no weakening of tuple/scope/consumption/expiry checks |
| `aos_core/queue.py` | Preserve durable dispatch rules; bind temporary identities to the same admission path |
| `aos_core/connectors.py` | Explicit temporary local-file registration/read scope; keep exclusive create and existing fixtures |
| `aos_core/validation.py` | Truthful result profile; independent exact-byte result observation |
| `tests/test_scaffold.py` | Exact manifest grows to 21 files; preserve regression assertions |
| `README.md`, `CONTRACTS.md` | Entry usage, temporary limits and precise claims |

All other source paths, notebook owners, legacy files, dev, historical records
and scratch remain unchanged. No migration of historical fixture stores.

New A scratch R: `W/.first-core-scratch/k2-integration/run-001/`.
New A development record: `W/.first-core-state/K2-INTEGRATION-RUN-001.json`.
Only R and this record may receive non-source writes. R contains baseline and
candidate manifests, reviewed sandbox policy, exact source copy under
`copy/first_core/`, synthetic fixture inputs, command outputs, final review,
and `launch/LAUNCH.md` with B's exact command/input/source/policy bindings.
Existing tests' relative scratch paths must resolve inside R through that copy.
No cleanup of existing material; absent targets and non-symlink containment are
required. The new integration/actual roots and A record were absent at preparation.

## 6. Required implementation semantics

1. Use the source-bound K1 → preview result; preserve criterion and scope meaning.
   Source-code manifest identity is distinct from the output candidate `ABSENT`
   or its observed digest. Bind both. Never treat every absent file as the same
   subject: action includes exact absolute target and effect.
2. Preserve the read-only preview's declared/observed distinctions and limits.
   The temporary entry accepts only reviewed explicit local inputs under its
   request-directory ceiling; no arbitrary commands, dynamic imports, discovery,
   credentials or new model/API calls. Validate syntax, types, finite numbers,
   path containment, hashes, current time and decision consistency before service
   initialization. Unknown/missing/mismatched inputs deny the affected action.
3. Bind Human-grant transcription to parent, child, revision, package/source/input
   hashes, exact command/action, risk profile, path scope, expiry and source/order.
   Do not store `approved:true` as a trusted capability or auto-convert supplied
   claims. No hidden native-conformance flag; temporary/manual provenance must
   survive into state, operation and final report.
4. Separately cover service writes before creating store/lock/registration/queue.
   Initialize only a genuinely new root; partial/existing history requires
   reconciliation and STOP, not a new blank task. One active parent and one
   COMMAND; no general backlog, EVENT follow-up or new independent goal.
5. Preserve existing C-006A local tuple checks, lifecycle transition, subset scope,
   envelope single use and durable consumption before effect. Read current local
   authority and use actual current time at admission, not an indefinitely reused
   caller timestamp. Manual current-chat check remains outside this local proof.
6. Action is only create-exclusive `target.txt`, one byte `7` (hex `37`), no
   overwrite. Queued admission goes through existing checks; do not call the
   connector directly to bypass admission. Unknown effect stops, without retry.
7. Preserve result-before-ACK and independent read-only validation. Queue ACK,
   matching JSON or success exit alone never closes the criterion. Denied/stale
   input cannot mutate target; distinguish legitimate prior service writes from
   forbidden admission/consumption/effect changes.
8. Use bounded reversible HOW inside these files. If compatibility requires an
   additional path or material architecture/authority change, report it and stop
   that dependent change. Do not implement native proof in a custom substitute.

## 7. A checks and final candidate

| Case | Required observation |
|---|---|
| Valid temporary integration fixture | K1 bindings → registration → durable enqueue → fresh admission → one effect → independent exact-byte result |
| No decision, wrong package/task/source/action/scope, revoked/expired record | Denial before effect; missing/invalid admission cannot be promoted by labels |
| Changed source/input/state/event/candidate, bool revisions, stale envelope | Exact mismatch denial; no consumption or target mutation on refused admission |
| Fixture/preview input used as launch input | No implicit promotion to temporary execution authority |
| Replay/duplicate operation, existing target, unresolved operation | No second effect/overwrite; report existing result or blocker without automatic retry |
| Symlink, path escape, outside root, malformed/oversized input | Reject before affected read/write; no unintended filesystem writes |
| Partial store, unavailable lock, lost result | Explicit UNKNOWN/STOP; never initialize over history or simulate recovery |
| Independent oracle negative control | Incorrect byte/path is detected; writer output and queue ACK cannot satisfy it |
| Regression | Full final-candidate suite, including all existing 107 test definitions; preserve 83 support cases and Entry preview behavior |
| Profile/reporting | Manual supervision explicit; native capture/resume remain UNKNOWN/NOT_RUN, no full K2 PASS |

Expected outputs must be defined independently of implementation output. Negative
authorities in tests are synthetic records, not native Human cancellation tests.
Fault injection is permitted only in disposable A fixtures; no native process
interruption, approval probe or actual B target is involved.

P = `/Users/muhammed/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3.12`.
Expected executable SHA256:
`ac60cfe0268614638d0ffa35f3b0284fc7b3a11482723793455e17eeb278509e`.
Use standard library only. Recheck executable, OS and exact environment before
execution; no install. Clear PYTHONPATH/PYTHONHOME, disable bytecode; capture only
relevant nonsecret environment values. Cwd is R/copy/first_core.

Future A command forms (R/P expand only to the paths above):

```text
/usr/bin/sandbox-exec -f R/checks.sb P -B -m aos_core --help
/usr/bin/sandbox-exec -f R/checks.sb P -B -m unittest discover -s tests -p 'test_*.py' -v
/usr/bin/sandbox-exec -f R/checks.sb P -B -m aos_core k2 supervised-run --input R/inputs/positive/request.json
```

The last command uses a synthetic A fixture, explicitly reported as such.
Review checks.sb before use: deny network, writes only R, source/state outside R
read-only; no broad filesystem escalation. Per-child timeout at most 60 seconds,
at most four owned children, one effectful owner. Record all exact commands and
results. Run full suite on final bytes; if unavailable, report NOT_RUN and withhold
B launch readiness. Fresh independent read-only reviewer never repairs candidate.

Proposed separate A budget: 60 active minutes / 30 effectful dispatches / 8 MiB
new source/state/scratch. The bytes allowance includes full-suite fixture copies
and retained corrective evidence (the previous 4 MiB run approached its limit).
One run, two-hour expiry from start. Historical usage is preserved; historical
parent remainder stays UNKNOWN. This package allocates nothing before a grant.
Update the new development record after material transitions, not every tool call.

## 8. B launch manifest to deliver after A

Child: `K2-FIRST-EFFECT-001`, parent `AOS-FIRST-CORE-001`.
B root: `W/.first-core-scratch/k2-actual/run-001/`.
Only product target: `B/target.txt`, initially ABSENT, exclusive one-byte creation.
New development record: `W/.first-core-state/K2-ACTUAL-RUN-001.json`.

A must deliver one concrete `R/launch/LAUNCH.md` with no unresolved execution
placeholders: exact frozen 21-file candidate, input templates/bytes and hashes,
accepted task criteria, Human-grant fields to be supplied by the actual decision,
exact expanded argv/cwd/environment, sandbox bytes/hash, observer argv, timeouts,
service file list and budgets. It includes a concise ready-to-send B grant.
Do not invent future source digests or a Human decision now. Future grant time and
source identity are genuine launch-time facts; preserve them separately from
frozen inputs so filling them cannot silently change the approved candidate.

B preparation after its separate grant may create its exact input/source-copy/
policy/output files, root, `store.json`, `store.lock`, bounded `publication-*.json`
and record, all listed in that manifest. The product child may write only its
store/lock/publications/target; its source copy, inputs and grant transcription
are read-only in its sandbox. Executor output capture is separately scoped.
No source changes or cross-root writes. Target creation is the sole product
effect; service initialization/persistence and evidence writes are also explicitly
authorized effects, not hidden under that description.

Recheck current Human grant, manifest, base/branch, root absence and lack of
competing work immediately before B. Execute one queued operation, independently
read the target with a separate read-only process using an expected byte/path
fixed before execution, compare state/ledger/operation and changed-path inventory,
then fresh read-only review and STOP. Do not invoke a replay against the real
target; duplicate controls belong to A fixtures. Create-exclusive success alone
does not prove generic exactly-once execution or native duplicate protection.

Proposed separate B ceiling: 30 active minutes / 15 effectful dispatches / 4 MiB,
one run, two-hour expiry. Not allocated by A or by this preparation.

## 9. Stop conditions, remaining gaps and next action

Stop the affected run on expired/missing grant, baseline or input drift,
unexpected path/provider access, competing writer, uncertain effect, interruption,
budget exhaustion, required new dependency/contract boundary, or repeated failure
without discriminating evidence. Ordinary bounded correction/recheck is permitted
only within a future A grant; B never repairs source or retries an uncertain effect.
No App Server/probes, new external access, sudo, installation, automatic recovery,
Git actions (including Commit after acceptance), K3 or Release.

Remaining implementation HOW: representation/glue inside §5, sandbox and observer
details frozen after review. Mutable bindings: actual run start/expiry, current
grant source/order, OS/environment and fresh filesystem observations. Runtime
evidence: new integration path/tests/effect NOT_RUN; native capture/current
authority binding UNKNOWN; automatic resume and host conformance remain unproven.
No new Product/Architecture policy decision is required to prepare A. Its exact
execution grant and B's later exact effect grant remain Human-only.

Next bounded action: authorize **A only**, binding this package's SHA256, manual
limitations, RP-FIRST-CORE-LOCAL-R1, its separate budget, §5 paths, existing App
model route and fresh read-only reviewer. Stop after the reviewed candidate and
completed B launch manifest. No current implementation or launch authorization.
